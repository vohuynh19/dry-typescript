import { uiReactDesignSystem } from './ui-react-design-system';

describe('uiReactDesignSystem', () => {
  it('should work', () => {
    expect(uiReactDesignSystem()).toEqual('ui-react-design-system');
  });
});
